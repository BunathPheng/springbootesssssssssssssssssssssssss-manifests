---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootesssssssssssssssssssssssss                  # Ingress name
  labels:
    app: springbootesssssssssssssssssssssssss
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootesssssssssssssssssssssssss.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootesssssssssssssssssssssssss               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
